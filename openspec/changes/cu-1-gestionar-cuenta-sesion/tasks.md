## 1. Persistencia y API de cuenta

- [x] 1.1 Antes de instalar, verificar compatibilidad declarada de cada dependencia con Node `24.11.1`, npm `11.6.2`, NestJS `11.1.6` y TypeORM `0.3.28`; fijar versiones compatibles de Passport/JWT, bcrypt, Zod y `nestjs-zod` sin usar `latest`, sin `@nestjs/typeorm` y conservando el único `package-lock.json` raíz. Extender `ApiConfiguration` con secreto JWT, vencimiento y costo bcrypt obligatorios, y publicar en `@primer-parcial/contracts` los esquemas/tipos de solo `email + password`, con email `trim().toLowerCase()`, email válido y password de mínimo 8 caracteres/máximo 72 bytes UTF-8 sin composición obligatoria. Verificar compatibilidad registrada, pruebas de configuración/contratos y `npm run typecheck`. [account-session: Un visitante puede registrar una cuenta con email y password válidos] [account-session: Un visitante puede iniciar sesión mediante email y password válidos] Archivos: `package.json`, `package-lock.json`, `apps/api/package.json`, `packages/contracts/**`, `apps/api/src/config/**` y pruebas afectadas.
- [x] 1.2 Implementar la entidad `User` auditable con UUID, email normalizado único y hash de contraseña, el `DataSource` de migraciones y una migración TypeORM explícita; crear un proveedor manual/lazy de usuarios que no inicialice PostgreSQL durante bootstrap y mantener `synchronize: false`. Verificar que la migración crea la tabla/restricción de email en PostgreSQL real de prueba, que ningún test usa los valores de desarrollo y que PostgreSQL caído no bloquea `createApiApplication`. [account-session: Un visitante puede registrar una cuenta con email y password válidos] [account-session: La API conserva health cuando PostgreSQL no está disponible] Archivos: `apps/api/src/users/**`, `apps/api/src/database/**`, `apps/api/src/migrations/**`, scripts/configuración de pruebas y pruebas de integración.
- [x] 1.3 Implementar el módulo de autenticación, hashing bcrypt, validación Zod, mapeo seguro de errores, emisión JWT, estrategia Passport, guardia y consulta protegida de sesión actual; documentar solo registro, login, sesión actual y Bearer JWT en Swagger, sin endpoint logout. Verificar con unitarias de hash/token/errores y Supertest de email inválido, password fuera de límites, registro/duplicado, login válido, email inexistente y password incorrecta con la misma respuesta pública, token anónimo/inválido/vencido, PostgreSQL no disponible, `/api/docs-json` sin logout, y la regresión GIVEN PostgreSQL caído WHEN inicia Nest THEN `/api/health` responde `503 {"status":"unavailable"}` con la API atendiendo. [account-session: Un visitante puede registrar una cuenta con email y password válidos] [account-session: Un visitante puede iniciar sesión mediante email y password válidos] [account-session: Las rutas privadas requieren una sesión JWT vigente] [account-session: Las operaciones de cuenta y sesión se documentan como parte de la API principal] [account-session: La API conserva health cuando PostgreSQL no está disponible] Archivos: `apps/api/src/auth/**`, `apps/api/src/app.module.ts`, `apps/api/src/app.ts`, `apps/api/test/**` y contratos afectados.

## 2. Landing, sesión y rutas privadas

- [x] 2.1 Implementar el cliente tipado de autenticación con formularios de solo `email + password` y almacenamiento de JWT exclusivamente en `sessionStorage`, incluyendo envío Bearer, limpieza de sesión y estados de carga/error. Verificar con Vitest que registro/login consumen los contratos, ninguna contraseña se persiste, el token se adjunta a la consulta protegida y logout lo elimina sin solicitar un endpoint backend. [account-session: Un visitante puede iniciar sesión mediante email y password válidos] [account-session: Una persona puede cerrar la sesión local actual] Archivos: `apps/web/src/auth/**`, componentes de prueba y contratos afectados.
- [x] 2.2 Reemplazar la página base por la landing CASE y añadir páginas Astro e islas Preact de registro e inicio de sesión con formularios de email/password etiquetados, foco, errores seguros y navegación pública, sin campos de username o perfil. Verificar con `@testing-library/preact` la interacción de formulario y, en revisión de componentes, etiquetas, foco y estados de error. [account-session: Un visitante puede registrar una cuenta con email y password válidos] [account-session: La experiencia pública y de autenticación es accesible y responsive] Archivos: `apps/web/src/pages/**`, `apps/web/src/components/**`, estilos y tests afectados.
- [x] 2.3 Añadir el área privada mínima y su guardia de cliente: consultar la sesión actual protegida antes de mostrar contenido, redirigir una sesión ausente/expirada a una ruta pública y exponer logout local, sin simular proyectos. Verificar con Vitest las rutas autorizada, anónima y expirada, y que logout elimina el área privada y el token en el cliente. [account-session: Las rutas privadas requieren una sesión JWT vigente] [account-session: Una persona puede cerrar la sesión local actual] Archivos: páginas e islas privadas de `apps/web/src/**` y pruebas afectadas.

## 3. Integración, evidencia y cierre del plan

- [x] 3.1 Crear los escenarios Playwright contra API, web y PostgreSQL reales para registro con email normalizado, login, acceso privado, logout local, acceso anónimo y token vencido; conservar la restauración segura de PostgreSQL de la suite existente y no usar mocks de la API. Verificar `npm run test:e2e` con Chromium. [account-session: Un visitante puede registrar una cuenta con email y password válidos] [account-session: Un visitante puede iniciar sesión mediante email y password válidos] [account-session: Las rutas privadas requieren una sesión JWT vigente] [account-session: Una persona puede cerrar la sesión local actual] Archivos: `e2e/**`, `playwright.config.ts` solo si es estrictamente necesario y documentación de pruebas afectada.
- [ ] 3.2 Actualizar la guía de variables, migraciones y compatibilidad de dependencias, crear el modelo de amenazas mínimo, crear `docs/puds/use-cases/CU-1-cuenta-sesion.md` y reflejar evidencia real en `docs/STATUS.md`; actualizar reproducción limpia y CI solo para incluir las migraciones/pruebas reales de CU-1, realizar la revisión manual de accesibilidad/responsive y solicitar o registrar evidencia real de lint, tipos, tests, E2E y build sin inventar resultados. [account-session: La experiencia pública y de autenticación es accesible y responsive] [account-session: Las operaciones de cuenta y sesión se documentan como parte de la API principal] [account-session: La API conserva health cuando PostgreSQL no está disponible] Archivos: `docs/development/README.md`, `docs/security/**`, `docs/puds/use-cases/CU-1-cuenta-sesion.md`, `docs/STATUS.md`, `README.md`, reproducción/CI y documentación realmente afectada.
- [ ] 3.3 Revisar el diff, dependencias, migraciones, OpenAPI, pruebas y documentación contra proposal/specs/design; confirmar que no se añadieron username, perfil, proyectos, ownership, roles, invitaciones, refresh/revocación, recuperación de contraseña, verificación de email, OAuth, MFA, UML, colaboración, XMI, generación, IA u offline, y que `opencode.json` preexistente no se modifica por CU-1. Verificar con `openspec validate cu-1-gestionar-cuenta-sesion --strict`, `git diff --check` y revisión manual del alcance. [account-session: Un visitante puede registrar una cuenta con email y password válidos] [account-session: Un visitante puede iniciar sesión mediante email y password válidos] [account-session: Las rutas privadas requieren una sesión JWT vigente] [account-session: Una persona puede cerrar la sesión local actual] [account-session: La experiencia pública y de autenticación es accesible y responsive] [account-session: Las operaciones de cuenta y sesión se documentan como parte de la API principal] [account-session: La API conserva health cuando PostgreSQL no está disponible] Archivos: todos los modificados por `cu-1-gestionar-cuenta-sesion`.
