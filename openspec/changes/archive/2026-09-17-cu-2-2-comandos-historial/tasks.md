## 1. Closed public contracts

- [x] 1.1 Remediate and export in `packages/uml-domain/src/` the exact closed 27-kind `UmlCommand` union: every Create/Add uses `{kind, external contexts, value: intrinsic fields and ID}`; `CreatePackage.parentPackageId: string | null` is required (`null` root), and `CreateClass.packageId` and `CreateEnumeration.packageId` are required strings. Verify compile-time exhaustiveness and runtime rejection of legacy embedded context, missing context, flat/partial/extra/unknown, maps/paths/field-value/callback/unknown-spread/generic-patch shapes.
- [x] 1.2 Define public typed command/history results and the closed precondition-code contract, including `TARGET_NOT_FOUND`, `PARENT_NOT_FOUND`, `PARENT_MISMATCH`, `DUPLICATE_ID`, `DUPLICATE_NAME`, `DEPENDENCIES_EXIST`, and `INCOMPATIBLE_REFERENCE`; verify each code and result discriminant through contract tests rather than handler/UI text.

## 2. Executor and preconditions

- [x] 2.1 Remediate the private exhaustive executor in `packages/uml-domain/src/` to resolve required external context, combine it only internally, preserve intrinsic `value` ownership-free, and retain uniqueness, compatibility, isolated immutable candidates, and untouched document parts; verify all 27 command kinds have focused success coverage.
- [x] 2.2 Implement reject-only delete/remove checks for every listed dependent in the delta specification, including layout/profile references, with no implicit cascade or cleanup; verify each DeletePackage, DeleteClass, DeleteEnumeration, RemoveOperation, DeleteAssociation, DeleteGeneralization, RemoveAttribute, RemoveParameter, and RemoveEnumerationLiteral blocker returns `DEPENDENCIES_EXIST` without mutation.
- [x] 2.3 Implement full update behavior and failures for attribute, operation, parameter, association, and generation profile, including ownership mismatch/missing parents, all association editable properties and complete ends, and profile replacement preserving semantic/layout data; verify the dedicated success/failure matrix.
- [x] 2.4 Implement MoveNode as replacement of an existing node position only; verify nonexistent nodes reject and semantic/profile data remain unchanged.
- [x] 2.5 Run exactly one CU-2.1 `edit` validation over each complete candidate and reject blocking diagnostics; verify no alternate policy is selectable and validator diagnostics remain stable.
- [x] 2.6 Remediate runtime Create/Add envelope validation: only `{kind, required external parent/context IDs, value: intrinsic CompleteValue}`, including required `CreatePackage.parentPackageId: string | null`, `CreateClass.packageId: string`, and `CreateEnumeration.packageId: string`; no ownership is duplicated in `value`, and no executor-generated IDs/timestamps/randomness. Verify legacy embedded context, missing context, flat, partial, extra, unknown, map, path, field-value, callback, and spread shapes reject before candidate construction.
- [x] 2.7 Implement the exact `DUPLICATE_NAME` scopes: root/sibling packages; shared Class/Enumeration classifier namespace per package; attributes and non-overloaded operations per Class; parameters per Operation; literals per Enumeration; and nonempty association names globally. Verify each collision and independent domain, unnamed associations, and that duplicate generalizations use their existing appropriate structural code.
- [x] 2.8 Preserve CU-2.1 types, serialization, and validator semantics through early checks; verify each complete candidate receives exactly one `edit` validation and no other selectable policy.

## 3. Bus and atomicity

- [x] 3.1 Implement `UmlCommandBus` as the only public stateful mutation service with private state limited to `currentDocument`; follow the normative order shape, preconditions/dependencies, isolated candidate, one `edit` validation, next `revision + 1` document, then one current-only atomic commit. Verify deterministic execution without clock, UUID, random, or external-state access, and no stacks/accessor/availability/Undo/Redo/capacity behavior.
- [x] 3.2 Verify every malformed envelope, precondition, candidate-construction, and `edit` validation rejection preserves the exact `currentDocument`, revision, and timestamps; verify rejected commands do not alter the current-only state.

## 4. History

- [x] 4.1 Expand private bus state only to `{currentDocument, undoStack, redoStack}` with private immutable full `ProjectDocument` snapshots containing canonical UML, layout, profile, `createdAt`, `updatedAt`, and captured revision. Keep stacks, tuple, capacity, and history accessors non-public; set capacity exactly to 100; expose only readonly `currentDocument`, `canUndo`, and `canRedo`, where each availability property is true if and only if its corresponding private stack is nonempty.
- [x] 4.2 Implement accepted-submit history transition only after the existing executor and exactly one CU-2.1 `edit` validation accept a candidate: push prior current to bounded undo, evict exactly the oldest snapshot beyond 100, set accepted next current, clear redo, and atomically commit the derived tuple. Preserve the established accepted revision and timestamp rules; verify accepted branching clears redo and updates availability from the committed tuple.
- [x] 4.3 Implement rejected-submit tuple preservation: malformed-envelope, precondition, candidate-construction, and `edit` validation failures synchronously return `UmlCommandResult` while preserving current, both stacks, revision, timestamps, availability, and redo contents exactly. Verify rejection neither enters history nor clears redo, does not throw, and does not invoke a history transition.
- [x] 4.4 Export exactly `UmlHistoryResult = {kind:'restored'; operation:'undo'|'redo'; document:ProjectDocument} | {kind:'unavailable'; operation:'undo'|'redo'}` and implement synchronous `undo(): UmlHistoryResult` and `redo(): UmlHistoryResult`. Unavailable navigation is a full-tuple no-op that returns its exact `unavailable` member, never `null`, `undefined`, an alternative shape, or a throw. Available Undo/Redo atomically moves the newest snapshot between bounded stacks and current, restores captured timestamps and revision without creating a command or incrementing revision, returns its exact `restored` member, and never invokes executor or validator; verify branching, accepted redo clearing, rejected redo retention, exact restoration, and the 100-entry boundary.

## 5. Integration and closure

- [x] 5.1 Finalize exports only: expose the public command contracts, `UmlCommandBus`, `readonly currentDocument`, `readonly canUndo`, `readonly canRedo`, `submit(command: unknown): UmlCommandResult`, `undo(): UmlHistoryResult`, `redo(): UmlHistoryResult`, and the exact `UmlHistoryResult` union; retain executor, stacks, tuple, and capacity internals private and exclude adapter dependencies. Verify package typechecking and an export-consumer test.
- [x] 5.2 Add only remediation regression and contract tests for external-context Create/Add envelopes: legacy embedded context, missing context, flat/partial/extra/unknown shapes, and synchronous unknown submission; isolated duplicate namespaces and nonempty association-name collision; every delete blocker; update failures/successes; association/profile behavior; serializable/parseable resulting documents; exact rejection tuple preservation; command catalogue, IDs/codes, validation, determinism; exact 100-entry history; readonly availability iff stack nonempty; exact restored/unavailable union members; unavailable no-op/no-throw; branching; accepted redo clearing; rejected redo retention; and direct Undo/Redo restoration without executor or validator. Verify the package test command passes.
- [x] 5.3 Update only documentation authorized by the active CU after actual implementation evidence exists; verify it does not claim unexecuted checks or out-of-scope functionality.
- [x] 5.4 Run the approved CU checks after implementation, including strict OpenSpec validation, and record only actual results; verify the final diff excludes UI/canvas, persistence, network, realtime, XMI, generation, AI, dependencies, and unrelated refactors.
