## 1. Persistencia y API de cuenta

- [x] 1.1 Actualizar `@primer-parcial/contracts` con esquemas/tipos diferenciados: registro de solo `firstName`, `lastName`, `email` y `password`; login de solo `email` y `password`; y cuenta/sesión segura con `id`, `firstName`, `lastName` y `email`. Nombres y apellidos se recortan y no pueden quedar vacíos; email conserva `trim().toLowerCase()` y password conserva mínimo 8 caracteres/máximo 72 bytes UTF-8 sin composición adicional. Mantener la configuración JWT/bcrypt y el único lockfile raíz, sin dependencias nuevas. Verificar contratos, configuración y typecheck. [account-session: Un visitante puede registrar una cuenta con datos básicos válidos] [account-session: Un visitante puede iniciar sesión mediante email y password válidos] Archivos: `packages/contracts/**`, `apps/api/src/config/**` y pruebas afectadas.
- [x] 1.2 Evolucionar `users` mediante una nueva migración TypeORM aditiva con `first_name` y `last_name` nullable para preservar cuentas existentes; actualizar entidad y DataSources sin `synchronize`, recreación de tabla ni borrado de cuentas. Verificar en PostgreSQL real de prueba que filas previas se conservan, ambas columnas existen y registros nuevos persisten nombres/apellidos válidos. Mantener el proveedor lazy y comprobar que PostgreSQL caído no bloquea `createApiApplication`. [account-session: Un visitante puede registrar una cuenta con datos básicos válidos] [account-session: La API conserva health cuando PostgreSQL no está disponible] Archivos: `apps/api/src/users/**`, `apps/api/src/database/**`, `apps/api/src/migrations/**`, scripts/configuración de pruebas y pruebas de integración.
- [x] 1.3 Actualizar registro, hashing bcrypt, validación Zod, mapeo seguro de errores y consulta de sesión para aceptar nombres/apellidos solo en registro, autenticar solo con email/password y devolver de forma segura `id`, `firstName`, `lastName` y `email`; preservar JWT, guardia, Bearer Swagger y ausencia de logout. Verificar unitarias de validación/hash/token/errores y Supertest de nombres/apellidos válidos, vacíos o solo espacios, persistencia y sesión segura, login de dos credenciales, tokens anónimo/inválido/vencido, PostgreSQL no disponible, `/api/docs-json` sin logout y regresión health `503`. [account-session: Un visitante puede registrar una cuenta con datos básicos válidos] [account-session: Un visitante puede iniciar sesión mediante email y password válidos] [account-session: Las rutas privadas requieren una sesión JWT vigente] [account-session: Las operaciones de cuenta y sesión se documentan como parte de la API principal] [account-session: La API conserva health cuando PostgreSQL no está disponible] Archivos: `apps/api/src/auth/**`, `apps/api/src/app.module.ts`, `apps/api/src/app.ts`, `apps/api/test/**` y contratos afectados.

## 2. Landing, sesión y rutas privadas

- [x] 2.1 Actualizar el cliente tipado para enviar el contrato de registro con nombres/apellidos/email/password y mantener login de solo email/password, JWT exclusivo en `sessionStorage`, Bearer, limpieza de sesión y estados de carga/error. Verificar con Vitest que nombres/apellidos no vacíos se validan, ninguna contraseña se persiste, el token se adjunta a sesión actual y logout lo elimina sin solicitar backend. [account-session: Un visitante puede registrar una cuenta con datos básicos válidos] [account-session: Un visitante puede iniciar sesión mediante email y password válidos] [account-session: Una persona puede cerrar la sesión local actual] Archivos: `apps/web/src/auth/**`, componentes de prueba y contratos afectados.
- [x] 2.2 Actualizar la landing CASE y la pantalla de registro con campos etiquetados de nombres, apellidos, email y password; mantener login con email/password solamente. Mostrar errores públicos, foco y navegación pública, sin username ni perfil. Verificar con `@testing-library/preact` nombres/apellidos válidos, vacíos o solo espacios, registro exitoso, error público y que login conserva dos campos. [account-session: Un visitante puede registrar una cuenta con datos básicos válidos] [account-session: Un visitante puede iniciar sesión mediante email y password válidos] [account-session: La experiencia pública y de autenticación es accesible y responsive] Archivos: `apps/web/src/pages/**`, `apps/web/src/components/**`, estilos y tests afectados.
- [x] 2.3 Actualizar el área privada y su guardia cliente para consultar y mostrar al menos el nombre de una sesión confirmada, sin convertirla en perfil ni proyectos; conservar redirección de sesión ausente/expirada y logout local. Verificar con Vitest sesión autorizada con nombres/apellidos, anónima, inválida y expirada, y que logout elimina token y área privada. [account-session: Las rutas privadas requieren una sesión JWT vigente] [account-session: Una persona puede cerrar la sesión local actual] Archivos: páginas e islas privadas de `apps/web/src/**` y pruebas afectadas.

## 3. Integración, evidencia y cierre del plan

- [x] 3.1 Actualizar los escenarios Playwright contra API, web y PostgreSQL reales para registro con nombres/apellidos válidos, normalización de email, nombres/apellidos vacíos, login de solo email/password, sesión privada que muestra nombre, logout local, acceso anónimo y token vencido; conservar restauración segura de PostgreSQL y no usar mocks de API. Verificar `npm run test:e2e` con Chromium. [account-session: Un visitante puede registrar una cuenta con datos básicos válidos] [account-session: Un visitante puede iniciar sesión mediante email y password válidos] [account-session: Las rutas privadas requieren una sesión JWT vigente] [account-session: Una persona puede cerrar la sesión local actual] Archivos: `e2e/**`, `playwright.config.ts` solo si es estrictamente necesario y documentación de pruebas afectada.
- [x] 3.2 Actualizar guía, migraciones, modelo de amenazas, documento CU-1, STATUS, reproducción y CI con la evolución nullable de nombres/apellidos y evidencia real de sus pruebas; realizar revisión manual accesible/responsive con los cuatro campos de registro y la vista privada con nombre, y registrar lint, tipos, tests, E2E y build sin inventar resultados. [account-session: Un visitante puede registrar una cuenta con datos básicos válidos] [account-session: La experiencia pública y de autenticación es accesible y responsive] [account-session: Las operaciones de cuenta y sesión se documentan como parte de la API principal] [account-session: La API conserva health cuando PostgreSQL no está disponible] Archivos: documentación y automatización realmente afectadas por CU-1.
- [x] 3.3 Revisar el diff, dependencias, migración evolutiva, OpenAPI, pruebas y documentación contra proposal/specs/design; confirmar que no se añadieron username, perfil, proyectos, ownership, roles, invitaciones, refresh/revocación, recuperación de contraseña, verificación de email, OAuth, MFA, UML, colaboración, XMI, generación, IA u offline, y que `opencode.json` preexistente no se modifica por CU-1. Verificar con `openspec validate cu-1-gestionar-cuenta-sesion --strict`, `git diff --check` y revisión manual del alcance. [account-session: Un visitante puede registrar una cuenta con datos básicos válidos] [account-session: Un visitante puede iniciar sesión mediante email y password válidos] [account-session: Las rutas privadas requieren una sesión JWT vigente] [account-session: Una persona puede cerrar la sesión local actual] [account-session: La experiencia pública y de autenticación es accesible y responsive] [account-session: Las operaciones de cuenta y sesión se documentan como parte de la API principal] [account-session: La API conserva health cuando PostgreSQL no está disponible] Archivos: todos los modificados por `cu-1-gestionar-cuenta-sesion`.
