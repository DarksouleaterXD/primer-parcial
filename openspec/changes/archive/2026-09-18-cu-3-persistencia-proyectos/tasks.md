## 1. Persistencia y esquema backend

- [x] 1.1 Crear en `apps/api/src/**` la entidad TypeORM `uml_projects` y su migracion explicita con `id`, `owner_id`, `name`, `revision`, `document` JSONB y auditoria; agregar FK `owner_id -> users.id` con cascade, restriccion de revision no negativa e indice `(owner_id, updated_at DESC, id)`, manteniendo `synchronize: false`, y verificar la migracion contra PostgreSQL y su esquema/FK/indice.
- [x] 1.2 Implementar en `apps/api/src/**` el limite repository/storage de proyectos que ensambla una unica representacion `ProjectDocument` desde/hacia la fila JSONB, usando serializador y parser de `@primer-parcial/uml-domain` sin tablas UML ni segundo modelo, y verificar el round-trip documento -> DB -> documento con modelo, layout y perfil intactos.
- [x] 1.3 Cubrir en pruebas de migracion/repositorio de `apps/api/test/**` la creacion del esquema, ownership por FK, borrado en cascada, listado owner-scoped y rechazo/observabilidad segura de JSONB corrupto, verificando que no se expongan entidades ni errores raw de TypeORM/PostgreSQL.

## 2. API, ownership y concurrencia

- [x] 2.1 Definir en `packages/contracts/src/**` y `apps/api/src/**` los envelopes Zod tipados `ProjectSummary`, `ProjectSnapshot`, validaciones publicas y resultados seguros de conflicto para las operaciones aprobadas bajo `/api/projects`; verificar que los DTOs no acepten `ownerId`, los contratos no dupliquen el parser UML y Swagger documente las respuestas publicas.
- [x] 2.2 Implementar `POST /api/projects` en `apps/api/src/**`, protegido por `JwtAuthGuard`, para crear desde `name` valido un `ProjectDocument` inicial con identidad, owner, auditoria y revision `0` derivadas por servidor; verificar mediante Supertest creacion autenticada, rechazo 401/400, ausencia de `ownerId` cliente y que un payload invalido no cree filas.
- [x] 2.3 Implementar `GET /api/projects` en `apps/api/src/**`, protegido por JWT, para devolver solo `ProjectSummary` propios con su revision vigente y sin documentos completos; verificar mediante Supertest que dos usuarios no descubran proyectos ajenos y que la solicitud sin sesion no exponga datos.
- [x] 2.4 Implementar `GET /api/projects/:projectId` y el adaptador de reapertura idempotente en `apps/api/src/**`, con parser de lectura antes del mapeo de respuesta; verificar mediante Supertest get/reopen del documento completo y que recurso ajeno o inexistente produzca el mismo `404` seguro sin contenido, owner, revision ni estado revelado.
- [x] 2.5 Implementar `PATCH /api/projects/:projectId/name` en `apps/api/src/**` con nombre validado y `expectedRevision`, mediante update transaccional condicionado por `id + owner_id + revision`; verificar mediante Supertest exito, payload invalido, dueño distinto/inexistente como `404`, conflicto `409` seguro y que un stale request no cambie nombre, documento, revision ni timestamps.
- [x] 2.6 Implementar `PUT /api/projects/:projectId/document` en `apps/api/src/**` con `expectedRevision`, parser y `validateProjectDocument(..., "save")`, verificaciones de identidad/owner/nombre/revision/timestamps inmutables y reemplazo transaccional condicionado por `id + owner_id + revision`; verificar mediante Supertest documento valido, documentos estructural o semanticamente invalidos como `400` sin escritura parcial, ajeno/inexistente como `404` y conflicto `409` seguro sin last-write-wins.
- [x] 2.7 Implementar `DELETE /api/projects/:projectId` en `apps/api/src/**` con `expectedRevision` y delete transaccional condicionado por `id + owner_id + revision`; verificar mediante Supertest `204` para owner/revision vigente, `404` indistinguible para ajeno o inexistente, `409` seguro para stale y ausencia de eliminacion o mutacion ante rechazo.
- [x] 2.8 Completar en `apps/api/test/**` las pruebas focalizadas de servicio/repositorio/API para clasificar condicionales de cero filas mediante lookup solo `(id, owner_id)` dentro de la transaccion, incluyendo escrituras concurrentes, rollback ante fallo y la garantia de que `409` no contiene revision, documento, owner, timestamps ni pistas internas.

## 3. Frontera web

- [x] 3.1 Crear en `apps/web/src/**` el cliente/repositorio tipado de proyectos que use exclusivamente la API, session storage existente para Bearer, envelopes estrictos y outcomes tipados para `401`, `400`, `404` y `409`; verificar con pruebas de repositorio/mock fetch rutas, metodos, headers, serializacion y parsing de respuestas sin acceso a PostgreSQL o TypeORM.
- [x] 3.2 Integrar en `apps/web/src/**` el seam aprobado create/reopen -> parser `ProjectDocument` -> nuevo `UmlCommandBus` -> controlador/view-model local y save explicito de `bus.currentDocument` con la ultima revision aceptada; verificar con pruebas de integracion web que una reapertura crea un bus nuevo y un save solo actualiza la revision local desde el snapshot aceptado.
- [x] 3.3 Definir en la frontera UX ya existente de `apps/web/src/**` el tratamiento explicito de outcomes `404` y `409`, sin merge ni sobrescritura automatica; verificar que no se agreguen timers, effects, callbacks de comandos o navegacion que hagan autosave, ni historial persistido, realtime o colaboracion.

## 4. Seguridad, round-trip y regresion

- [x] 4.1 Agregar pruebas cross-layer en `packages/uml-domain/test/**`, `apps/api/test/**` y `apps/web/test/**` para el round-trip completo de `ProjectDocument` entre dominio, JSONB, contrato y cliente, preservando modelo, layout, perfil, identidad, revision y timestamps, y verificando la reutilizacion del parser/validador de guardado.
- [x] 4.2 Agregar regresiones de aislamiento y concurrencia en `apps/api/test/**` que demuestren no descubribilidad de proyectos de otro usuario, `404` indistinguible para ajeno/inexistente, conflicto reproducible y ausencia de escritura parcial o mutacion tras una revision obsoleta.
- [x] 4.3 Ejecutar y ampliar solo donde corresponda las regresiones de `packages/uml-domain/test/**` y `apps/web/test/**` para confirmar que los 27 comandos, `UmlCommandBus`, Undo/Redo y el workspace CU-2.3 siguen locales y funcionan sin base de datos, sin cambiar sus contratos o semantica.

## 5. Integracion y cierre

- [x] 5.1 Exponer los contratos y modulos requeridos desde `packages/contracts/src/**`, `apps/api/src/**` y `apps/web/src/**`, y verificar que las dependencias respeten dominio -> aplicacion -> adaptador/controlador y que la web solo alcance persistencia mediante API.
- [x] 5.2 Crear y actualizar `docs/puds/use-cases/CU-3-proyectos-persistentes.md`, `docs/STATUS.md`, `docs/PROJECT_CONTEXT.md`, `docs/architecture/README.md` y `docs/development/README.md` con contratos, migracion, ownership, concurrencia, flujos, pruebas, evidencia real, comandos y limites de CU-3; verificar que describan el comportamiento implementado sin afirmar resultados no ejecutados.
- [x] 5.3 Ejecutar lint, typecheck, pruebas unitarias/integracion/API/web, build, `openspec validate cu-3-persistencia-proyectos --strict`, `git diff --check` y `scripts/reproduce-clean.ps1`, y registrar resultados reales, entorno y cualquier bloqueo en la documentacion de CU-3.
- [x] 5.4 Revisar el alcance final contra `proposal.md`, `specs/uml-project-persistence/spec.md` y `design.md`, verificando que no se agregaron autosave, WebSocket/realtime, colaboracion/presencia/roles/share, Undo/Redo persistido, XMI, generacion, IA ni cambios a los 27 comandos o a la semantica de `UmlCommandBus`.
