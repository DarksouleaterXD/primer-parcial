## 1. Contratos del documento y subconjunto UML

- [x] 1.1 Crear `packages/uml-domain` como workspace TypeScript independiente de UI/backend, con scripts de lint, typecheck, test y build coherentes con el monorepo. Definir `schemaVersion`, IDs, reloj/fábricas inyectables para tests, `ProjectDocument`, `CanonicalUmlModel`, `DiagramLayout` y contratos del perfil de generación. Verificar que el paquete no importe Astro, Preact, NestJS, TypeORM, D3, ELK, WebSocket ni IA. [uml-domain-validation: El proyecto UML usa un documento canónico versionado y serializable] [uml-domain-validation: El modelo UML y el layout permanecen desacoplados] Archivos: `packages/uml-domain/**`, scripts raíz solo si son necesarios.
- [x] 1.2 Implementar el subconjunto UML aprobado: paquetes, clases, atributos/propiedades, operaciones/parámetros, visibilidad, tipos primitivos/referenciados, enumeraciones, asociaciones con dos extremos y agregación `none/shared/composite`, multiplicidades y generalización. Las referencias usan IDs y los nombres no actúan como identidad. Añadir fixtures deterministas de cada familia. [uml-domain-validation: El subconjunto UML 2.5.1 aprobado se expresa con contratos cerrados] [uml-domain-validation: Las multiplicidades y referencias son deterministas] Archivos: `packages/uml-domain/src/model/**`, `packages/uml-domain/test/fixtures/**` o estructura equivalente.
- [x] 1.3 Implementar el perfil de generación separado del UML puro con metadatos de clase, atributo y `defaultSort` referenciados por ID. Probar que modificar perfil no modifica entidades UML y que round-trip conserva ambos agregados. [uml-domain-validation: El perfil de generación permanece separado del UML puro] Archivos: `packages/uml-domain/src/profile/**` y pruebas.

## 2. Serialización y validación

- [x] 2.1 Implementar creación, serialización y parsing de `ProjectDocument` versión 1. El parser debe rechazar forma/versión no soportada sin devolver documentos parciales y el round-trip debe preservar UML, layout, perfil, revisión y metadatos. No implementar migraciones de documento futuras. [uml-domain-validation: El proyecto UML usa un documento canónico versionado y serializable] Archivos: `packages/uml-domain/src/document/**` y pruebas.
- [x] 2.2 Implementar un único validador determinista con diagnósticos `severity/code/message/path/elementId`, orden estable y reglas mínimas para IDs duplicados, nombres requeridos, referencias, multiplicidades, ciclos de paquetes/generalización, layout huérfano y perfil huérfano. Un fallo interno no puede producir un resultado válido. [uml-domain-validation: La validación produce diagnósticos estables y reutilizables] Archivos: `packages/uml-domain/src/validation/**` y pruebas.
- [x] 2.3 Implementar las políticas `edit`, `save`, `import` y `generate` como selección de bloqueo sobre el mismo resultado de validación, sin crear persistencia, XMI ni generación. Probar que una misma infracción conserva código/path entre políticas y que warnings no bloquean por defecto. [uml-domain-validation: Las políticas de validación separan contexto sin duplicar el motor] Archivos: `packages/uml-domain/src/validation/**` y pruebas.

## 3. Integración, evidencia y cierre de CU-2.1

- [x] 3.1 Ejecutar y corregir pruebas del nuevo dominio y regresión raíz: lint, typecheck, test y build. Confirmar round-trip, fixtures completos, invariantes, IDs, referencias, multiplicidades, versionado, layout y perfil. No requerir PostgreSQL, navegador ni Internet para las pruebas unitarias de `uml-domain`. [uml-domain-validation: El proyecto UML usa un documento canónico versionado y serializable] [uml-domain-validation: El subconjunto UML 2.5.1 aprobado se expresa con contratos cerrados] [uml-domain-validation: La validación produce diagnósticos estables y reutilizables] Archivos: pruebas y configuración realmente afectadas.
- [x] 3.2 Actualizar `CU-2-modelado-uml-manual.md`, `docs/STATUS.md`, arquitectura/desarrollo si cambian comandos públicos, y registrar únicamente resultados reales. Ejecutar reproducción limpia si el nuevo workspace o lockfile cambian el flujo reproducible; documentar cualquier desviación. [uml-domain-validation: El proyecto UML usa un documento canónico versionado y serializable] Archivos: documentación y automatización realmente afectadas.
- [x] 3.3 Revisar alcance final contra proposal/spec/design. Confirmar que CU-2.1 no añadió `UmlCommandBus`, Undo/Redo, D3/ELK/Shoelace/Nanostores, canvas, páginas de editor, persistencia PostgreSQL, proyectos persistentes, realtime, XMI, generación, IA, voz, imagen ni offline integral. Ejecutar `openspec validate cu-2-1-modelo-validacion --strict` y `git diff --check`. [uml-domain-validation: El modelo UML y el layout permanecen desacoplados] [uml-domain-validation: Las políticas de validación separan contexto sin duplicar el motor] Archivos: todos los modificados por `cu-2-1-modelo-validacion`.
