## 1. Bloque 1 — CI y verificaciones reproducibles

Detener y revisar este bloque antes de preparar una reproducción limpia.

- [x] 1.1 Añadir únicamente `@playwright/test`, un único proyecto Chromium, la configuración Playwright y el script raíz `test:e2e`, sin crear escenarios disponible/no disponible; verificar instalación desde el lockfile, sintaxis de configuración y que el script tenga un destino real. [base-executable-assurance: El flujo web se comprueba en un navegador real] Archivos: `package.json`, `package-lock.json`, configuración Playwright y script raíz afectado.
- [x] 1.2 Crear el workflow mínimo de CI que use Node 24/npm 11, `npm ci`, el PostgreSQL existente, la instalación explícita de Chromium y los comandos raíz de calidad, E2E y build, sin despliegue, secretos ni publicación; verificar sintaxis del workflow y que cada comando corresponda a la raíz, sin declarar aún evidencia E2E satisfactoria. [base-executable-assurance: La integración continua verifica la base ejecutable] Archivos: `.github/workflows/**`, `package.json`, `compose.yaml`, configuración Playwright.
- [x] 1.3 Configurar el ciclo de vida de Chromium y PostgreSQL en CI para que el job posea los servicios que inicia y los fallos de calidad, E2E o build hagan fallar el workflow; verificar configuración y tooling sin implementar aún escenarios E2E reales. [base-executable-assurance: La integración continua verifica la base ejecutable] Archivos: workflow CI, configuración Playwright y documentación de evidencia.

## 2. Bloque 2 — Reproducción desde entorno limpio

Detener y revisar este bloque antes de añadir pruebas de navegador.

- [x] 2.1 Implementar una receta o automatización aislada que cree un directorio temporal fuera de `D:\project-planning` desde solo el snapshot versionado elegido, excluya `node_modules`, `dist`, `.astro`, cobertura y outputs generados, y nunca use `git clean` ni lea `.env` reales; verificar precondiciones, aislamiento, origen versionado y mensajes de bloqueo. [base-executable-assurance: La base se puede reproducir desde archivos versionados] Archivos: script o guía de reproducción, `package.json` si requiere script, documentación de desarrollo.
- [x] 2.2 Integrar en la receta el arranque reversible del PostgreSQL Compose existente, los orígenes sintéticos y la comprobación de health sin modificar el checkout; verificar que solo elimina su propio directorio temporal, que la base saludable y el health disponible se observen desde la copia limpia y que un snapshot sin los cambios no produzca evidencia. [base-executable-assurance: La base se puede reproducir desde archivos versionados] Archivos: receta de reproducción, `compose.yaml`, `.env.example`, guías afectadas.
- [x] 2.3 Ejecutar o solicitar evidencia de una reproducción limpia real; registrar snapshot, comandos, versiones, resultado, bloqueos y recuperación sin declarar éxito si Docker, el puerto fijo, las dependencias o un snapshot incompleto impiden la comprobación. [base-executable-assurance: La base se puede reproducir desde archivos versionados] Archivos: `docs/puds/use-cases/CU-0-inicializar-base.md`, `docs/STATUS.md` cuando haya resultado real.

## 3. Bloque 3 — E2E y navegador/manual

Detener y revisar este bloque antes del cierre documental.

- [x] 3.1 Implementar exclusivamente el escenario E2E Chromium disponible que inicie la API y web existentes con orígenes explícitos y compruebe `API disponible` contra PostgreSQL real; verificar que no use mocks, polling ni reintentos. [base-executable-assurance: El flujo web se comprueba en un navegador real] Archivos: pruebas E2E y scripts raíz afectados.
- [x] 3.2 Implementar exclusivamente los escenarios E2E de PostgreSQL no disponible y recuperación: antes de actuar registrar el estado inicial del servicio `postgres` del Compose del proyecto, no tocar instancias externas, recargar o abrir página nueva para comprobar `API no disponible` y restaurar en `finally` el estado original incluso ante fallo; verificar iniciado/healthy si lo estaba y detenido si lo estaba originalmente, sin `docker compose down -v`, borrado de contenedores, volúmenes o datos. [base-executable-assurance: El flujo web se comprueba en un navegador real] [base-executable-assurance: La verificación preserva el estado local de PostgreSQL] Archivos: pruebas E2E, helpers estrictamente necesarios y evidencia de recuperación.
- [x] 3.3 Ejecutar la revisión manual de navegador pendiente, independiente de E2E, con URL, fecha, estado disponible, estado no disponible, recuperación y resultado real; registrar fallo como bloqueo, no como aprobación implícita. [base-executable-assurance: El flujo web se comprueba en un navegador real] Archivos: `docs/puds/use-cases/CU-0-inicializar-base.md` y documentación de desarrollo afectada.

## 4. Bloque 4 — Cierre documental y comprobación final de alcance

Detener y revisar este bloque antes de solicitar CU-1.

- [x] 4.1 Ejecutar o solicitar evidencia real del workflow CI desde un repositorio y runner autorizados; si no existe runner autorizado o falla, registrar el bloqueo y mantener CU-0 en implementación, sin crear remoto, hacer push, publicar ni fingir evidencia. [base-executable-assurance: La integración continua verifica la base ejecutable] Archivos: workflow CI, `docs/puds/use-cases/CU-0-inicializar-base.md`, `docs/STATUS.md`.
- [x] 4.2 Reunir evidencia de checks raíz, build, CI, reproducción limpia, E2E automatizada y revisión manual; actualizar únicamente el documento CU-0, STATUS y guías realmente afectadas, y marcar CU-0 terminado solo si todos los criterios son reales y completos. [base-executable-assurance: La integración continua verifica la base ejecutable] [base-executable-assurance: La base se puede reproducir desde archivos versionados] [base-executable-assurance: El flujo web se comprueba en un navegador real] [base-executable-assurance: La verificación preserva el estado local de PostgreSQL] Archivos: `docs/puds/use-cases/CU-0-inicializar-base.md`, `docs/STATUS.md`, `README.md`, `docs/development/README.md` y documentación afectada.
- [x] 4.3 Revisar diff, dependencias, workflow, scripts, pruebas y documentación contra proposal/specs/design para confirmar que no se añadieron despliegue, publicación, remoto, CU-1, auth, UML, colaboración, XMI, generación, IA, capacidades offline ni refactorizaciones preventivas; verificar además que `docs/development/validate-cu-0.1.mjs` permanezca histórico e intacto. [base-executable-assurance: La integración continua verifica la base ejecutable] Archivos: todos los modificados por la change y `openspec/changes/cu-0-3-calidad-reproducibilidad-cierre/**`.
- [x] 4.4 Corregir exclusivamente `.github/workflows/ci.yml` para fijar Node `24.11.1`, asegurar y mostrar npm `11.6.2`, e imprimir ambas versiones antes de `npm ci`; verificar sintácticamente y en Linux aislado que el único `package-lock.json` se consume sin modificarlo, sin agregar `@emnapi/core` ni `@emnapi/runtime` como dependencias directas, sin `npm audit fix` ni cambios funcionales. Preparar el commit/push solo como comandos manuales y obtener una nueva ejecución real de GitHub Actions; marcar esta tarea completa únicamente cuando el runner autorizado informe Node `24.11.1`, npm `11.6.2` y el workflow finalice correctamente. [base-executable-assurance: La integración continua verifica la base ejecutable] Archivos: `.github/workflows/ci.yml` y documentación de evidencia afectada.
