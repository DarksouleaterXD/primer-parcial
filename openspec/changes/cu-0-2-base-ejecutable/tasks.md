## 1. Bloque 1 — Estructura y contratos

Detener y revisar este bloque antes de iniciar el backend.

- [x] 1.1 Crear los workspaces reales `apps/web`, `apps/api` y `packages/contracts`, y ajustar `package.json`, configuraciones TypeScript y manifiestos de workspace para limitarse a CU-0.2; verificar la estructura y la ausencia de paquetes vacíos o capacidades fuera de alcance. [base-executable-workspaces: Los workspaces contienen solo los componentes de CU-0.2] Archivos: `package.json`, `tsconfig.base.json`, `apps/web/package.json`, `apps/api/package.json`, `packages/contracts/package.json` y configuraciones de cada workspace.
- [x] 1.2 Definir en `packages/contracts` el único contrato exportado de health `{ status: "available" | "unavailable" }` y su prueba de contrato; verificar que web y API puedan importarlo sin dependencias de framework. [api-health: La API expone su salud con dependencia verificada] Archivos: `packages/contracts/src/**`, `packages/contracts/test/**`, `packages/contracts/tsconfig.json`.
- [x] 1.3 Declarar exclusivamente las dependencias de Astro/Preact, NestJS/Express, TypeORM/PostgreSQL, Swagger y las herramientas de prueba/lint/tipos exigidas por los requisitos; fijar en las devDependencies raíz `@nestjs/cli` exactamente en `11.0.24`, sin `latest`, rangos ni overrides transitivos. Instalar desde la raíz y verificar que no haya `EBADENGINE` de Angular DevKit, que Node `24.11.1` se mantenga y que exista un único `package-lock.json` sin lockfiles en workspaces. [base-executable-workspaces: La instalación usa un único lockfile raíz] Archivos: `package.json`, `apps/web/package.json`, `apps/api/package.json`, `packages/contracts/package.json`, `package-lock.json`.
- [x] 1.4 Exponer en la raíz comandos `dev`, `test`, `lint`, `typecheck` y `build` que deleguen trabajo real a los workspaces creados; verificar sus destinos mediante los manifiestos y dejar la ejecución de build para evidencia de usuario o CI. [base-executable-workspaces: La raíz ofrece comandos verificables de CU-0.2] Archivos: `package.json` y manifiestos de workspace.

## 2. Bloque 2 — Backend ejecutable

Detener y revisar este bloque antes de iniciar el frontend.

- [x] 2.1 Configurar `apps/api` como API NestJS 11 sobre Express con TypeORM y variables de entorno para PostgreSQL y un único origen web permitido; verificar que el bootstrap cargue la configuración sin inicializar ni esperar TypeORM, por lo que la API inicia aun con PostgreSQL caída, y que rechace origen vacío o `*`. [api-health: La API expone su salud con dependencia verificada] [api-health: El acceso cruzado usa un origen configurado] Archivos: `apps/api/src/**`, `apps/api/.env.example`, `.env.example`, configuraciones Nest/TypeORM.
- [x] 2.2 Implementar `GET /api/health` con el contrato compartido mediante un `DataSource` TypeORM efímero por solicitud: inicializarlo, ejecutar una comprobación real y cerrarlo si tuvo éxito; verificar `200` con `{ "status": "available" }` cuando PostgreSQL responda y `503` con `{ "status": "unavailable" }` sin datos sensibles ante un error de inicialización o consulta. [api-health: La API expone su salud con dependencia verificada] Archivos: `apps/api/src/health/**`, `apps/api/src/**` de arranque y `packages/contracts/src/**`.
- [x] 2.3 Publicar Swagger de la API principal en `/api/docs` y `/api/docs-json`, describiendo `GET /api/health` y los estados `200`/`503`; verificar ambos recursos contra la aplicación iniciada. [api-health: La salud se documenta como parte de la API principal] Archivos: `apps/api/src/**` de bootstrap y health.
- [x] 2.4 Añadir pruebas backend y de contrato para bootstrap de API con PostgreSQL no disponible, health `503` ante error TypeORM, health `200` con PostgreSQL disponible, ausencia de datos sensibles, CORS permitido/no permitido y documentos OpenAPI; verificar las pruebas con el script del workspace cuando PostgreSQL esté operativo, registrando un bloqueo real si no lo está. [api-health: La API expone su salud con dependencia verificada] [api-health: La salud se documenta como parte de la API principal] [api-health: El acceso cruzado usa un origen configurado] Archivos: `apps/api/test/**`, `packages/contracts/test/**`, configuración Jest.

## 3. Bloque 3 — Frontend ejecutable

Detener y revisar este bloque antes de las comprobaciones integradas.

- [ ] 3.1 Crear la aplicación Astro con TypeScript y Preact, y entregar a la isla `ApiStatus` el origen público de la API mediante entorno; verificar que la página carga la isla y que el origen no queda hardcodeado. [web-api-status: La web consulta el health configurado] Archivos: `apps/web/src/pages/**`, `apps/web/src/components/ApiStatus.*`, `apps/web/.env.example`, configuración Astro/TypeScript.
- [ ] 3.2 Implementar el ciclo único de `ApiStatus`: comprobando durante la solicitud, disponible solo ante `200` y contrato válido, y no disponible ante `503` o respuesta inválida; verificar cada transición con pruebas de componente. [web-api-status: La web comunica el ciclo de disponibilidad] Archivos: `apps/web/src/components/ApiStatus.*`, `apps/web/test/**`.
- [ ] 3.3 Incorporar cancelación por timeout y manejo de rechazo de red en `ApiStatus`, sin reintento, polling ni estado saludable falso; verificar timeout y error de red con pruebas controladas. [web-api-status: La web trata timeout y fallos de red como no disponibles] Archivos: `apps/web/src/components/ApiStatus.*`, `apps/web/test/**`.

## 4. Bloque 4 — Verificación y documentación

Detener y revisar este bloque antes de solicitar autorización para cualquier trabajo posterior.

- [ ] 4.1 Ejecutar o solicitar a usuario/CI la evidencia de `lint`, `typecheck`, `test` y `build` de los comandos raíz; verificar que cada resultado corresponda a los workspaces de CU-0.2 y registrar resultados reales, sin declararlos como cierre de CU-0.3. [base-executable-workspaces: La raíz ofrece comandos verificables de CU-0.2] Archivos: `package.json`, salidas de evidencia y documentación de CU-0.2.
- [ ] 4.2 Realizar dos comprobaciones integradas acotadas e independientes: cargar una isla nueva con PostgreSQL, API y red disponibles para observar disponible; después, con PostgreSQL, API o red en la condición no disponible, recargar la página o ejecutar una isla nueva para observar no disponible. Verificar y registrar URL, fecha y resultado sin secretos, sin polling, retry ni actualización automática de `ApiStatus`. [api-health: La API expone su salud con dependencia verificada] [api-health: El acceso cruzado usa un origen configurado] [web-api-status: La web consulta el health configurado] [web-api-status: La web comunica el ciclo de disponibilidad] [web-api-status: La web trata timeout y fallos de red como no disponibles] Archivos: documentación de CU-0.2 y evidencia autorizada.
- [ ] 4.3 Actualizar exclusivamente la documentación relacionada con CU-0.2 para reflejar archivos, contrato, configuración, pruebas ejecutadas, manuales, bloqueos y deuda reales; verificar que `docs/puds/use-cases/CU-0-inicializar-base.md` y `docs/STATUS.md` no atribuyan cierre de CU-0 ni de CU-0.3. [base-executable-workspaces: Los workspaces contienen solo los componentes de CU-0.2] [api-health: La salud se documenta como parte de la API principal] [web-api-status: La web comunica el ciclo de disponibilidad] Archivos: `docs/puds/use-cases/CU-0-inicializar-base.md`, `docs/STATUS.md`, `README.md`, `docs/development/README.md` y ejemplos de entorno afectados.
- [ ] 4.4 Revisar diff, workspaces, dependencias y documentación para confirmar que no se incluyeron CI, Playwright completo, autenticación, UML, canvas, WebSockets, colaboración, XMI, generación, IA, voz, imágenes, offline completo, refactorizaciones preventivas ni paquetes futuros; verificar la lista contra `proposal.md`, `specs/` y este checklist antes de solicitar el siguiente CU. [base-executable-workspaces: Los workspaces contienen solo los componentes de CU-0.2] Archivos: todos los archivos modificados por CU-0.2 y `openspec/changes/cu-0-2-base-ejecutable/**`.
